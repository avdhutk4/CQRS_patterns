server:
  port: 8085


spring:
  application:
    name: PRODUCT-SERVICE
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/cqrs
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create